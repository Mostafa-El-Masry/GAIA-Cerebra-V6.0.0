import type { ReactNode } from "react";

export const metadata = {
  title: "GAIA | Eleuthia",
};

export default function EleuthiaLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
